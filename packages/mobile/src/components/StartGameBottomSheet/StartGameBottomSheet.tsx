import { View, Text } from "react-native";
import React from "react";

const StartGameBottomSheet = () => {
  return (
    <View>
      <Text>StartGameBottomSheet</Text>
    </View>
  );
};

export default StartGameBottomSheet;
